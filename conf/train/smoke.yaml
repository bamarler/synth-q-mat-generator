# Tiny training profile for CI / quick debug runs: make exp ARGS="train=smoke".
algorithm: ppo
total_steps: 200
n_envs: 2
batch_size: 32
learning_rate: 3.0e-4
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
kl_target: 0.02
checkpoint_every: 100
